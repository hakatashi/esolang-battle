extends layout

block content
  .container.content
    if contest.start > new Date()
      include partials/countdown.pug
    else
      h1 Rule
      != description.en
      
      hr(style={margin: '5em 0'})
      
      h1 ルール
      != description.ja
